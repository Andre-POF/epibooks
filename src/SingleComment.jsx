import {Row, Col, Form} from "react-bootstrap";
import AddComment from "./AddComment";

export default function (props) {
    const {currentRate, setCurrentRate, submitBtn, data, setNewComment} = props;
    return (
        <Row>
        <Col>
            <Form>
                <Form.Group>
                
                </Form.Group>
            </Form>
        </Col>
    </Row>
    )
}